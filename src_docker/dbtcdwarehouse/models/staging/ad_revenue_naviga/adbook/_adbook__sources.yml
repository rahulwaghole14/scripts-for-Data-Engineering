version: 2

sources:
  - name: adbook
    database: hexa-data-report-etl-prod
    schema: adw_stage
    tables:
      - name: adbook__adrevenue
        tests:
          - dbt_utils.unique_combination_of_columns:
              combination_of_columns:
                - campaign_id
                - line_id
                - month_unique_id
                - month_start_date
